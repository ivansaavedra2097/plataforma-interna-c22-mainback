generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id               String               @id @default(uuid())
  name             String
  surname          String
  email            String               @unique
  password         String
  active           Boolean              @default(true)
  createdAt        DateTime             @default(now())
  updatedAt        DateTime             @updatedAt
  platform_modules UserPlatformModule[]
  roles            UserRole[]
  code             UserValidationCode?
  phone_numbers    PhoneNumber[]
}

model Role {
  id          Int        @id @default(autoincrement())
  name        String     @db.VarChar(256)
  description String     @db.VarChar(256)
  users       UserRole[]
}

model PlatformModule {
  id          Int                  @id @default(autoincrement())
  name        String               @db.VarChar(256)
  description String               @db.VarChar(256)
  users       UserPlatformModule[]
}

model PhoneNumber {
  id           Int    @id @default(autoincrement())
  phone_number String @unique @db.VarChar(10)
  user_id      String
  user         User   @relation(fields: [user_id], references: [id])
}

model UserPlatformModule {
  user_id            String
  platform_module_id Int
  platform_module    PlatformModule @relation(fields: [platform_module_id], references: [id])
  user               User           @relation(fields: [user_id], references: [id])

  @@id([user_id, platform_module_id])
}

model UserValidationCode {
  id      Int    @id @default(autoincrement())
  code    String @db.VarChar
  user_id String @unique
  user    User   @relation(fields: [user_id], references: [id])
}

model UserRole {
  user_id String
  role_id Int
  role    Role   @relation(fields: [role_id], references: [id])
  user    User   @relation(fields: [user_id], references: [id])

  @@id([user_id, role_id])
}
